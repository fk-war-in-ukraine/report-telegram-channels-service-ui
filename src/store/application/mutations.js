export function setApplicationState (state, applicationState) {
  state.applicationState = applicationState
}

export function setShowErrorMessage (state, showErrorMessage) {
  state.showErrorMessage = showErrorMessage
}
